# AGENTS.md

本文件适用于整个仓库。所有在本项目中工作的 Agent 均须遵守以下约定。

## Git 工作流

- 每一轮产生文件修改后，都必须在本轮结束前创建一次 Git 提交，不得只保留未提交改动。
- 提交前先检查 `git status` 和差异内容，确认提交范围准确，并运行与改动相关的验证；涉及构建或页面内容时，至少运行 `npm run verify`。
- 每次提交只包含本轮任务直接相关的文件。不得顺带提交、覆盖或清理用户已有的无关改动。
- 使用清晰、具体、祈使语气的提交信息，推荐遵循 Conventional Commits，例如 `feat: add publication filters`、`fix: correct profile spacing`、`docs: document deployment workflow`。
- 不得使用 `git reset --hard`、强制推送或其他可能丢失现有工作的破坏性 Git 操作，除非用户明确要求。

## 页面边界

- 不设计、不实现学校网站的页头（包括校徽、学校名称、顶部导航等页头区域）。
- 页面导入学校系统后，页头会由学校系统自动生成；本项目只负责可嵌入学校模板的教师主页正文。
- 页面布局和样式不得为自建页头预留占位、间距或相关交互，也不得使用全局样式干扰学校系统生成的页头。
- 如需本地预览，只展示正文或使用明确标注为预览辅助的最小容器；预览辅助内容不得进入最终 CMS 正文片段。

## 构建与验证

- 论文数据以 `src/data/publications.json` 为唯一维护源，不直接手工修改由构建流程生成的论文列表。
- 修改源码、数据或样式后，运行 `npm run verify`，并确认生成的 `dist/` 与 `cms/` 交付内容保持同步。
- 最终交付应继续适配广西师范大学 SUDY/Webplus，由学校模板负责站点级页面框架。
